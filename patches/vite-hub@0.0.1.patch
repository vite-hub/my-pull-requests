diff --git a/dist/index.d.ts b/dist/index.d.ts
index 08199b98a010dda07e3cb6f9f9b12437ee3db996..1e4714ac12f09f0b574ecc025d3aeb9d5ee5accc 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -24,7 +24,7 @@ interface ViteHubOptions {
   browser?: boolean | BrowserModuleOptions;
   channels?: boolean | ChannelsVitePluginOptions;
   database?: boolean | DBModulePublicOptions;
-  email?: EmailVitePluginOptions;
+  email?: boolean | EmailVitePluginOptions;
   env?: false | EnvIntegrationOptions;
   kv?: boolean | KVModuleOptions;
   queue?: boolean;
diff --git a/dist/nuxt.js b/dist/nuxt.js
index a78b6ce5e825c0c8c03d297ed3cb209252319236..f4e84216ee9dc81dba0d898174f98ca072eb7dff 100644
--- a/dist/nuxt.js
+++ b/dist/nuxt.js
@@ -57,6 +57,17 @@ function configHandler(plugin) {
 	if (typeof plugin.config === "function") return plugin.config;
 	return plugin.config?.handler;
 }
+function closeBundleHandler(plugin) {
+	if (typeof plugin.closeBundle === "function") return plugin.closeBundle;
+	return plugin.closeBundle?.handler;
+}
+function providerOutputNitroModule(plugins) {
+	const names = /* @__PURE__ */ new Set(["@vite-hub/schedule/vite", "@vite-hub/workflow/vite"]);
+	const handlers = plugins.filter((plugin) => names.has(plugin.name)).map(closeBundleHandler).filter(Boolean);
+	return (nitro) => nitro.hooks.hook("compiled", async () => {
+		for (const handler of handlers) await handler.call({});
+	});
+}
 function queueNitroConfigHandler(plugin) {
 	return plugin.vitehub?.queue?.createNitroConfig;
 }
@@ -176,6 +187,14 @@ const viteHubNuxtModule = async function viteHubNuxtModule(inlineOptions, nuxt)
 	const existingNames = new Set(flattenPlugins(existing).map((plugin) => plugin.name).filter(Boolean));
 	const existingPluginsByName = new Map(flattenPlugins(existing).filter((plugin) => plugin.name).map((plugin) => [plugin.name, plugin]));
 	const installedPlugins = [...plugins.map((plugin) => existingPluginsByName.get(plugin.name) || plugin), ...flattenPlugins(existing).filter((plugin) => (plugin.name.startsWith("@vite-hub/") || plugin.name.startsWith("vite-hub/")) && !plugins.some((candidate) => candidate.name === plugin.name))];
+	const finalizeProviderOutputs = options.preset === "vercel" && !nuxt.options.dev ? providerOutputNitroModule(installedPlugins) : void 0;
+	const emailPlugin = installedPlugins.find((plugin) => plugin.name === "@vite-hub/email/vite");
+	await emailPlugin?.api?.prepareTypes?.({
+		projectRoot,
+		serverDirs: nuxt.options.serverDir ? [nuxt.options.serverDir] : void 0
+	});
+	const typesPlugin = installedPlugins.find((plugin) => plugin.name === "vite-hub/types");
+	await typesPlugin?.api?.prepareTypes?.(projectRoot);
 	nuxt.options.vite ??= {};
 	const viteConfig = nuxt.options.vite;
 	if (envConfig && Object.values(envConfig).some(Boolean)) {
@@ -210,6 +229,7 @@ const viteHubNuxtModule = async function viteHubNuxtModule(inlineOptions, nuxt)
 		await applyNitroConfig(installedPlugins, config, nuxt);
 		const alias = config.alias ??= {};
 		for (const [name, path] of Object.entries(generatedAliases)) alias[name] ??= path;
+		if (finalizeProviderOutputs) (config.modules ??= []).push(finalizeProviderOutputs);
 	});
 	if (options.agent) addVueImports(nuxt, "vite-hub/agent/vue", agentVueComposables);
 	if (options.auth) {
diff --git a/dist/src-ULOSMczT.js b/dist/src-ULOSMczT.js
index 6ba7a9bb30fdf196ad397f097fefb4b9a2bc9a74..c6dd1eaef4930a174b46c9b847d6651ee61aa309 100644
--- a/dist/src-ULOSMczT.js
+++ b/dist/src-ULOSMczT.js
@@ -711,6 +711,7 @@ function viteHubTypesPlugin() {
 	return {
 		name: "vite-hub/types",
 		enforce: "post",
+		api: { prepareTypes: writeViteHubTypes },
 		async configResolved(config) {
 			projectRoot = resolveViteHubProjectRoot(config.root);
 			await refreshGeneratedTypes();
@@ -1079,6 +1080,11 @@ function presetBlobOptions(plan, options, deploymentName = "vitehub-blob") {
 		default: throw new Error("[vitehub] Missing Blob adapter for deployment preset " + JSON.stringify(plan.preset) + ".");
 	}
 }
+function presetEmailOptions(plan, options) {
+	if (options !== true) return options;
+	if (plan.preset === "cloudflare") return { driver: "unemail/driver/cloudflare-email" };
+	throw new Error(`[vitehub] The ${JSON.stringify(plan.preset)} preset requires an explicit Email driver.`);
+}
 function vitehub(options) {
 	if (!options || typeof options !== "object") throw new TypeError("vitehub() requires a built-in deployment preset.");
 	const plan = resolveDeploymentPlan(options.preset);
@@ -1158,7 +1164,7 @@ function vitehub(options) {
 		nitroOwned: true
 	}));
 	if (options.email) plugins.push(hubEmail({
-		...options.email,
+		...presetEmailOptions(plan, options.email),
 		hosting: plan.nitroPreset,
 		runtimeEnvImport: "vite-hub/env/server"
 	}));
@@ -1180,6 +1186,7 @@ function vitehub(options) {
 		importBase: "vite-hub/realtime"
 	}));
 	if (options.schedule) plugins.push(hubSchedule({
+		...plan.preset === "vercel" ? { providerOutput: "standalone" } : {},
 		...options.schedule === true ? {} : options.schedule,
 		importBase: `${generatedImportBase}/schedule`,
 		providerImportAliases,
